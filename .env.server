# ========================================
# CONFIGURACIÓN PARA SERVIDOR DE PRODUCCIÓN
# ========================================
# Este archivo contiene la configuración para el servidor de la institución
# 
# INSTRUCCIONES DE USO:
# 1. Modifica los valores según tu servidor
# 2. Copia este archivo al servidor
# 3. Renómbralo a .env en el servidor
# 4. ¡NO subas este archivo a GitHub con datos reales!
# ========================================

# ========================================
# DJANGO SETTINGS
# ========================================

# Modo debug - SIEMPRE False en producción por seguridad
DEBUG=False

# Genera una nueva clave secreta DIFERENTE para producción:
# Ejecuta: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=GENERA-UNA-NUEVA-CLAVE-SECRETA-AQUI

# ========================================
# HOSTS Y CORS
# ========================================

# Reemplaza <IP_DEL_SERVIDOR> con la IP real del servidor
# Obtén la IP con: ipconfig (Windows) o hostname -I (Linux)
# Ejemplo: ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.50,horarios.institucion.edu
ALLOWED_HOSTS=localhost,127.0.0.1,<IP_DEL_SERVIDOR>

# URLs desde donde el frontend puede hacer peticiones
# Si frontend y backend están en el mismo servidor, usa la IP del servidor
# Ejemplo: CORS_ALLOWED_ORIGINS=http://192.168.1.50:8000
CORS_ALLOWED_ORIGINS=http://<IP_DEL_SERVIDOR>:8000

# ========================================
# CONFIGURACIÓN DE DESPLIEGUE
# ========================================

# Siempre False para servidor propio (True solo para Render.com)
RENDER=False

# ========================================
# BASE DE DATOS (PostgreSQL)
# ========================================

# Si tu servidor usa diferentes credenciales de base de datos, descomenta y configura:
# DATABASE_URL=postgresql://usuario:contraseña@127.0.0.1:5432/horarios_db

# ========================================
# NOTAS IMPORTANTES
# ========================================
# 1. Reemplaza todos los valores marcados con < >
# 2. Genera una SECRET_KEY nueva y única para producción
# 3. Verifica que DEBUG=False en producción
# 4. Ajusta las IPs según tu red local
# 5. Si usas dominio, agrégalo a ALLOWED_HOSTS
